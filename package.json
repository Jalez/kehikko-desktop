{
  "name": "kehikko-desktop",
  "private": true,
  "version": "0.1.0",
  "description": "A window on the Kehikot workbench, and the thing that owns the host's lifetime.",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "tauri": "tauri",
    "install-app": "bash dev/install.sh"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
